# plugin-stamp-library
Named terrain pieces saved in plugin storage, persisting across maps and exportable as JSON to share. Extended-terrain users keep libraries of hand-tuned ramps, bridges and cliff pieces and currently re-copy them from an old map every time. Clipboard plus storage plus a panel.
